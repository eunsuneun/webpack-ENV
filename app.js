// const alert = msg => window.alert(msg);
// new Promise();

const foo = "";
console.log(
  "---------------------------------------------------------매우 긴 문장 입니다.---------------------------------------------------------"
);
